root: ${SSM_ROOT}

binaries:
  llama_server: ${LLAMA_SERVER:-llama-server}
  trtexec: ${TRTEXEC:-/usr/src/tensorrt/bin/trtexec}
  python: ${SSM_PYTHON:-python3}

services:
  vlm:
    host: 127.0.0.1
    port: 8080
  video:
    host: 0.0.0.0
    port: 9080
    camera: /dev/video0
  train_api:
    port: 5000
    enabled: false
